{
  "name": "fintech-brain",
  "version": "0.1.0",
  "description": "CLI for publishing and installing shared fintech team knowledge and agent skills.",
  "type": "module",
  "bin": {
    "fintech": "./bin/fintech.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node ./bin/fintech.js",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
